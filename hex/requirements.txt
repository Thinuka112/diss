# Python deps for train_quality_cnn.py on a Hex GPU node.
# NOTE: PyTorch itself is installed separately by setup_and_run.sh so it can be
# matched to the node's CUDA version (see `nvidia-smi`). Do not pin torch here.
timm>=0.9.0
numpy>=1.24
pillow>=10.0
scikit-learn>=1.3
matplotlib>=3.7
# PLIP (vision-language backbone) experiment - train_quality_plip.py
transformers>=4.30
huggingface_hub>=0.16
